export interface LanguageOption {
  iso_639_1: string;       // mã ngôn ngữ chuẩn (dùng để gọi API)
  english_name: string;    // tên tiếng Anh
  name: string;            // tên bản địa hóa (Tiếng Việt, Español, etc.)
}
